ktor:
  application:
    modules:
      - com.routeplanner.api.ApplicationKt.module
  deployment:
    port: 8080

jwt:
  domain: "http://localhost:8080/"
  audience: "mobile-app"
  issuer: "http://localhost:8080/"
  realm: "route-planner-api"
  claimField: "sub"

database:
  url: "jdbc:sqlserver://localhost:1433;databaseName=route_planner_db;encrypt=true;trustServerCertificate=true"
  driver: "com.microsoft.sqlserver.jdbc.SQLServerDriver"
  user: "sa"
  password: "pepe123!"
